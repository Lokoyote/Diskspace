<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.diskspace" path="/org/gnome/shell/extensions/diskspace/">
    <key name="selected-mountpoint" type="s">
      <default>''</default>
      <summary>Default displayed mountpoint</summary>
      <description>
        Path of the mountpoint whose usage percentage stays shown in the
        panel indicator. An empty string means "automatic" (system disk, /).
      </description>
    </key>
  </schema>
</schemalist>
